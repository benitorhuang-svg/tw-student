# 任務清單：核心 Atlas 體驗

**輸入文件**：`/specs/001-core-atlas/` 內的規格、計畫、研究、資料模型與契約  
**前置條件**：`plan.md`、`spec.md`、`research.md`、`data-model.md`、`contracts/`  
**測試方式**：以靜態檢查、正式建置與 quickstart 手動驗證為主。  
**組織方式**：依資訊架構、互動一致性與可維護性拆分。

## 階段一：規格整理

**目的**：先把流水號式規格整理為較少、較穩定的主線文件。

- [x] T001 將 `specs/` 根目錄整理為 `001-core-atlas`、`002-platform-delivery` 與 `archive`
- [x] T002 建立 `specs/README.md` 與 `specs/archive/README.md`，說明新舊對照
- [x] T003 更新 `001-core-atlas` 的 `spec.md`、`plan.md`、`tasks.md`

---

## 階段二：資訊架構重排（使用者故事一，優先級 P1）

**目標**：把地圖回歸為左側篩選器，右側固定作為分析畫布。

**獨立驗證**：畫面改為左側 `頁籤 + 篩選 + 地圖`、右側 `卡片 + 圖表 + 表格`，且原有互動流程不退化。

- [x] T004 [US1] 移除獨立 `AtlasHeader` 版位並將頁面標題移入主內容區
- [x] T005 [US1] 將地圖移入左側控制欄，右側改為分析畫布
- [x] T006 [US1] 將 App 容器拆分為較小的版面元件

---

## 階段三：年份表達一致性（使用者故事三，優先級 P1）

**目標**：將主要年份文案統一為西元學年度顯示。

**獨立驗證**：檢查年份下拉、趨勢圖、異常表格與比較情境文案，確認不再只顯示 `113` 類型文字。

- [x] T007 [US3] 更新年份 formatter，使 UI 主顯示改為西元學年度
- [x] T008 [US3] 更新圖表、異常表格與情境標籤的年份文案

---

## 階段四：核心功能回歸檢查（使用者故事二 / 四，優先級 P2）

**目標**：確保版型重排後，URL 深連結、下鑽、排行、異常與學校工作台都仍可正常運作。

**獨立驗證**：執行 lint / build，並確認右側頁籤內容仍正確出現。

- [x] T009 [US2] 保留 URL / 地圖 / 右側分析面板的狀態同步
- [x] T010 [US4] 確認學校工作台與異常治理面板仍掛在右側主畫面

---

## 階段五：驗證與收尾

**目標**：確認優化完成且可交付。

- [x] T011 執行 `npm run lint`
- [x] T012 執行 `npm run build`
- [x] T013 執行 E2E 檢查主要流程

---

## 依賴與執行順序

- 階段一必須先完成，才可進入任何程式修改。
- URL 狀態同步與資料切片是本輪核心交付，可並行，但都依賴新的資料契約。
- 地圖視覺修正依賴資料切片與狀態同步完成後的最終資料流。
- skill 建立可在主要實作穩定後補上，避免寫入過時規範。
- 最終驗證必須在所有功能與文件同步後執行。