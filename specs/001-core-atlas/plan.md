# 實作計畫：核心 Atlas 體驗

**分支**: `001-interactive-edu-map` | **日期**: 2026-03-08 | **對應規格**: `/specs/001-core-atlas/spec.md`
**輸入來源**: `/specs/001-core-atlas/spec.md` 的現行主線需求

## 摘要

本輪實作要把現有互動地圖工作台整理成更可用的核心體驗。重點包含：

1. 將資訊架構收斂為「左側控制欄 / 右側分析畫布」。
2. 保持地圖下鑽、URL 深連結、正式資料切片與異常註記不退化。
3. 將學年度顯示統一改為西元學年度。
4. 以較小的容器元件重組版面，降低 `App.tsx` 的版面責任。

## 技術背景

**語言 / 版本**: TypeScript 5.9、React 19  
**主要依賴**: React、Vite、d3-geo、csv-parse、shpjs  
**資料層**: 由資料刷新腳本產出的靜態正式資料檔，並在前端進行分段載入與 URL 還原  
**測試方式**: ESLint、TypeScript build、手動 quickstart 驗證  
**目標平台**: 現代桌面與行動瀏覽器  
**專案型態**: 單頁前端應用  
**效能目標**: 正式資料導入後，首次可互動內容維持在可接受等待範圍內，篩選與層級切換維持主觀即時反應  
**限制條件**: 單頁面下鑽、繁體中文、優先使用靜態資料產物，不預設引入本地資料庫或後端服務  
**目前範圍**: 全台、縣市、鄉鎮市區、學校層級正式資料探索與分享

## 憲章檢查

*門檻：必須先通過，才可進入實作。*

- **互動探索優先**：通過。此次實作直接強化分享連結、地圖操作與視覺一致性。
- **先更新規格，再進行實作**：通過。已先將主規格整理為 `001-core-atlas` 與 `002-platform-delivery`。
- **正式資料可追溯與異常透明**：通過。規格已要求資料註記與異常揭露。
- **效能優化必須可驗證**：通過。將以資料產物、lint、build 與手動情境驗證。
- **邊界清楚，優先靜態交付**：通過。先採靜態切片與壓縮，不直接引入本地資料庫。
- **規格文件繁體中文**：通過。本輪文件全部維持繁中。

## 專案結構

### 規格文件

```text
specs/001-core-atlas/
├── plan.md
├── research.md
├── data-model.md
├── quickstart.md
├── contracts/
│   └── analytics-data-contract.md
└── tasks.md
```

### 原始碼與資料產物

```text
frontend/
├── public/
│   └── data/
├── scripts/
│   └── refresh-official-data.mjs
├── src/
│   ├── components/
│   ├── data/
│   ├── lib/
│   ├── App.tsx
│   ├── App.css
│   ├── index.css
│   └── main.tsx
├── package.json
├── tsconfig.json
└── vite.config.ts
```

### Skill 資產

```text
.github/
└── skills/
	└── taiwan-education-atlas/
		└── SKILL.md
```

## 架構決策

- 仍維持單一前端應用，因為目前需求重點是互動探索與靜態資料交付，而非多使用者即時查詢。
- 地理資料優先改成 TopoJSON 或等效壓縮產物，再於前端還原為可繪製資料，以降低邊界檔體積。
- 正式教育資料將拆分為「全台摘要層」、「縣市層摘要」、「學校細節層」等切片，避免每次進站都讀全量深層資料。
- URL 狀態同步由前端控制，不需要為此引入路由框架或後端 session。
- 前端版型拆分為 `AtlasControlRail`、`AtlasAnalysisPanel` 與既有地圖 / 分析元件，以符合原子化設計方向。
- 本地端資料庫暫不作為主方案，相關評估保留在 `specs/archive/003-local-db-prefetch-atlas/`。

## 複雜度追蹤

| 項目 | 為何需要 | 為何不採更簡單方案 |
|------|----------|--------------------|
| TopoJSON / 切片資料 | 正式資料量已明顯超過示範資料，需降低首次載入成本 | 若繼續直接載入完整 GeoJSON 與全量學校資料，效能會退化 |
| URL 狀態同步 | 需支援分享、重整還原、深連結 | 只用 local state 無法分享分析上下文 |
| 資料註記模型 | 正式資料存在缺年、停辦與改制差異 | 若不建模，只能在 UI 臨時硬寫，後續難維護 |