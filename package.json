{
  "name": "student-counting-analysis-tw",
  "private": true,
  "scripts": {
    "postinstall": "npm --prefix services/frontend install && npm --prefix services/backend install",
    "dev": "npm --prefix services/frontend run dev",
    "build": "npm --prefix services/frontend run build",
    "lint": "npm --prefix services/frontend run lint",
    "data:refresh": "npm --prefix services/backend run data:refresh"
  }
}
