{
  "name": "student-counting-analysis-tw",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev": "npm run dev -w frontend",
    "build": "npm run build -w frontend",
    "lint": "npm run lint -w frontend",
    "lint:architecture": "npm run lint:architecture -w frontend",
    "preview": "npm run preview -w frontend",
    "test:types": "npm run test:types -w frontend",
    "data:refresh": "npm run data:refresh -w backend",
    "data:refresh:tiles": "npm run data:refresh:tiles -w backend"
  }
}
